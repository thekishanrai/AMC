"use client";import{useState}from"react";import{IconSearch,IconX}from"@tabler/icons-react";export default function TopBar({search="",onSearch}:{search?:string;onSearch?:(v:string)=>void}){const[open,setOpen]=useState(false);return <header className="amc-top-wrap"><div className="amc-topbar"><div className="amc-wordmark">ANTI MONDAY CLUB</div><div className="amc-top-actions"><button type="button" aria-label={open?"Close search":"Search"} onClick={()=>{setOpen(!open);if(open)onSearch?.("")}}>{open?<IconX/>:<IconSearch/>}</button></div></div>{open&&<label className="amc-search"><IconSearch size={17}/><input autoFocus value={search} onChange={e=>onSearch?.(e.target.value)} placeholder="Search escapes"/></label>}</header>}
