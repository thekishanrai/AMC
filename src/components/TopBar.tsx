"use client";import Link from"next/link";import{useState}from"react";import{IconSearch,IconX}from"@tabler/icons-react";export default function TopBar({search="",onSearch,showSearch=true}:{search?:string;onSearch?:(v:string)=>void;showSearch?:boolean}){const[open,setOpen]=useState(false);return <header className="amc-top-wrap"><div className="amc-topbar"><Link href="/" className="amc-wordmark" aria-label="Anti Monday Club home">ANTI MONDAY CLUB</Link>{showSearch&&<label className="amc-search-desktop"><IconSearch size={16} stroke={2.5}/><input value={search} onChange={e=>onSearch?.(e.target.value)} placeholder="Search escapes, forts, lakes..." aria-label="Search escapes"/>{search&&<button type="button" aria-label="Clear search" onClick={()=>onSearch?.("")}><IconX size={14} stroke={3}/></button>}</label>}{showSearch&&<div className="amc-top-actions"><button type="button" aria-label={open?"Close search":"Search"} onClick={()=>{setOpen(!open);if(open)onSearch?.("")}}>{open?<IconX size={22} stroke={3}/>:<IconSearch size={22} stroke={3}/>}</button></div>}</div>{showSearch&&open&&<label className="amc-search"><IconSearch size={17}/><input autoFocus value={search} onChange={e=>onSearch?.(e.target.value)} placeholder="Search escapes"/></label>}</header>}
