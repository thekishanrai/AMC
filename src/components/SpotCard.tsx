"use client";import{IconBrandWhatsapp}from"@tabler/icons-react";import{formatDuration}from"@/lib/format";import{photoSrc}from"@/lib/photo";import type{Spot}from"@/types";export default function SpotCard({spot,distance,onOpen,onFocus,slug}:{spot:Spot;distance:number;onOpen:()=>void;onFocus:()=>void;slug?:string}){const photo=photoSrc(spot.photos?.find(Boolean));const share=()=>{const url=`${location.origin}/${spot.category}/${slug??""}`;window.open(`https://wa.me/?text=${encodeURIComponent(`${spot.name} - ${url}`)}`,"_blank","noopener,noreferrer")};return <article className="amc-spot-card" data-spot-id={spot.id} onPointerEnter={onFocus}><button type="button" className="amc-card-main" onClick={onOpen}><div className="amc-card-photo" style={photo?{backgroundImage:`url("${photo}")`}:undefined}><strong>{Math.round(distance)} KM AWAY</strong></div><div className="amc-card-copy"><h3>{spot.name}</h3><p>{spot.region??spot.category}</p><div className="amc-stat-row"><span>{Math.round(distance)} km</span>{spot.time_by_car_minutes!=null&&<span>{formatDuration(spot.time_by_car_minutes)}</span>}{spot.difficulty&&<span>{spot.difficulty}</span>}</div></div></button><button type="button" aria-label={`Share ${spot.name} on WhatsApp`} className="amc-save-round" onClick={share}><IconBrandWhatsapp size={18}/></button></article>}
