"use client";import{CATEGORIES}from"@/lib/categories";import type{Category}from"@/types";export const DEFAULT_DISTANCE_KM=100;const PRESETS=[25,50,100,200,500];export default function CategoryChips({active,onChange,maxDistanceKm,onDistanceChange}:{active:Category|"all";onChange:(c:Category|"all")=>void;maxDistanceKm:number;onDistanceChange:(k:number)=>void}){return <div className="amc-filter-block"><p className="amc-label">HOW FAR?</p><div className="amc-presets">{PRESETS.map(k=><button key={k} type="button" onClick={()=>onDistanceChange(k)} className={maxDistanceKm===k?"is-active":""}>{k===500?"fuck it. farther":`${k} km`}</button>)}</div><p className="amc-label">Categories</p><div className="amc-category-row">{CATEGORIES.map(({key,label,icon:Icon})=><button key={key} type="button" onClick={()=>onChange(key)} className={active===key?"is-active":""}><Icon size={14}/>{label}</button>)}</div></div>}
