"use client";import{useState}from"react";import{createPortal}from"react-dom";import{IconX,IconLocation}from"@tabler/icons-react";import{QUICK_CITIES,type QuickCity}from"@/lib/locationOverride";export default function LocationPicker({label,confirmed,onPick,onUseGps,locatingGps}:{label:string;confirmed:boolean;onPick:(c:QuickCity)=>void;onUseGps:()=>void;locatingGps:boolean}){const[open,setOpen]=useState(false);const modal=open&&typeof document!=="undefined"?createPortal(<div className="amc-location-modal" onClick={()=>setOpen(false)}><div className="amc-location-backdrop"/><div className="sheet-enter amc-location-sheet" onClick={e=>e.stopPropagation()}><div className="flex items-center justify-between"><div><h2 className="font-headline text-[18px]">Select your location</h2><p className="text-xs">Currently: {label}</p></div><button onClick={()=>setOpen(false)} aria-label="Close"><IconX/></button></div><button onClick={()=>{onUseGps();setOpen(false)}} className="amc-gps"><IconLocation/>{locatingGps?"Finding you…":"Use my precise location"}</button><div className="amc-city-grid">{QUICK_CITIES.map(c=><button key={c.name} onClick={()=>{onPick(c);setOpen(false)}}>{c.name}</button>)}</div></div></div>,document.body):null;return <><button type="button" onClick={()=>setOpen(true)} className="glass-solid flex items-center gap-2 rounded-full px-4 py-2.5 text-[13px]">{confirmed?label:"Select your location"}<span className="amc-location-caret">▼</span></button>{modal}</>}
