import{NextRequest,NextResponse}from"next/server";export const revalidate=86400;export async function GET(request:NextRequest){const source=request.nextUrl.searchParams.get("url");if(!source||!/^https:\/\/(www\.)?instagram\.com\/(p|reel)\//i.test(source))return new NextResponse(null,{status:400});try{const html=await fetch(source,{headers:{"User-Agent":"Mozilla/5.0"},next:{revalidate:86400}}).then(r=>r.text());const m=html.match(/<meta[^>]+property=["']og:image["'][^>]+content=["']([^"']+)/i)??html.match(/<meta[^>]+content=["']([^"']+)["'][^>]+property=["']og:image/i);if(!m?.[1])return new NextResponse(null,{status:404});const image=await fetch(m[1].replace(/&amp;/g,"&"),{next:{revalidate:86400}});if(!image.ok||!image.body)return new NextResponse(null,{status:404});return new NextResponse(image.body,{headers:{"Content-Type":image.headers.get("content-type")??"image/jpeg","Cache-Control":"public, max-age=86400, stale-while-revalidate=604800"}})}catch{return new NextResponse(null,{status:502})}}
